using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShipDefultController : ControllerBase
{
    // Start is called before the first frame update
    void Start()
    {
        ControllerBaseStart();

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
